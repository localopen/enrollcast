image: rocker/r-base

before_script:
  - apt-get update

create-pages:
  script:
    - mkdir .public
    - cp -r docs/* .public
    - mv .public public
  pages: true
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
